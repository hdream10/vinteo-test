// Что было плохо и почему:
// Использование однобуквенных переменных, таких как `e`, делает код трудным для понимания.
// Название функции `pay` повторяется с переменной `pay`, что может создавать путаницу и ухудшает читаемость.

// Что стало лучше и почему:
// Переименовали переменную `e` на более понятное `employee`, чтобы повысить читаемость и улучшить контекст.
// Переименовали переменную `pay` в `payment`, чтобы избежать путаницы с одноименной функцией и улучшить понимание контекста.

const pay = () => {
  for (let employee of employees) {
    if (employee.isPayday()) {
      const payment = employee.calculatePay();
      employee.deliverPay(payment);
    }
  }
};
