// Что было плохо и почему:
// Используется магическое число, что делает его сложным для понимания и изменения.
// Отсутствует типизация для параметра `user`, что может привести к ошибкам.

// Что стало лучше и почему:
// Магическое число вынесено в константу, что улучшает читаемость кода и облегчает его изменение.
// Добавлен тип для параметра `user`, что улучшает безопасность кода и позволяет избежать ошибок.

const MIN_AGE = 18;

function processUserData(user: User) {
  // Получаем данные пользователя
  const userData = getUserData(user);

  // Проверяем данные пользователя
  if (userData.age >= MIN_AGE) {
    // Обрабатываем данные
    processUser(user);
  } else {
    // Выводим ошибку
    console.error('User is under 18');
  }
}
