// Не совсем понятно для чего этот класс
// и какой контекст выполнения.
// Если подразумевается, что нужно добавить `_` (private)/ `#` (protected) к названию свойств,
// то это не обязательно, так как это уже регулируется модификаторами.

class Car {
  private make: string;
  private model: string;
  private year: number;
  private color: string;
  private price: number;
  private mileage: number;
}
